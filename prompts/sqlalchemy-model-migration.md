---
title: SQLAlchemy model and migration
use_when: Adding ORM models and Alembic migrations
related_skills:
  - sqlalchemy-db
  - testing-mariadb
  - tdd-workflow
  - python-style
---

# Prompt: SQLAlchemy model and migration

Copy everything below the line into chat.

---

Add SQLAlchemy 2.x model **`<ENTITY>`** and Alembic migration.

Constraints:
- Dialect-agnostic types (`Mapped[]` / `mapped_column`)
- TDD: failing persistence test first; integration against MariaDB in Docker
- Review autogenerated migration for portability
- black + ruff; type hints
- Newest stable SQLAlchemy and Alembic

Include upgrade/downgrade notes and test commands (`docker compose --profile test up` + pytest).
